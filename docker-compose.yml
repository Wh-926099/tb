version: '3.8'

services:
  lumina:
    build:
      context: .
      args:
        GEMINI_API_KEY: ${GEMINI_API_KEY}
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: lumina-game
