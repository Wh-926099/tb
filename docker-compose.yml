version: '3.8'

services:
  lumina:
    build:
      context: .
      args:
        DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: lumina-game
